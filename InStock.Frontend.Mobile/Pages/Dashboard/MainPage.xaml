<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InStock.Frontend.Mobile.Pages.Dashboard.MainPage">

    <CollectionView ItemsSource="{Binding Items}"
                    SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                    SelectionMode="Single"
                    VerticalOptions="FillAndExpand"
                    HorizontalOptions="FillAndExpand">
        <CollectionView.Header>
            <StackLayout BackgroundColor="{StaticResource Primary}" HeightRequest="240" Padding="20,40">
                <Label FontSize="Large" TextColor="{StaticResource White}" Text="InStock" HorizontalOptions="Center" />
            </StackLayout>
        </CollectionView.Header>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout>
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Command}" />
                    </StackLayout.GestureRecognizers>
                    <Label Text="{Binding Title}" FontSize="Large" />
                    <Label Text="{Binding Subtitle}" FontSize="Medium" />
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>