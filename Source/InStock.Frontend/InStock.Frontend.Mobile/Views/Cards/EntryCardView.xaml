<?xml version="1.0" encoding="utf-8" ?>
<frames:PrimaryCardFrame
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:frames="clr-namespace:InStock.Frontend.Mobile.Views.Frames"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:InStock.Frontend.Core.ViewModels.Cards;assembly=InStock.Frontend.Core"
    x:DataType="vm:EntryCardViewModel"
    x:Class="InStock.Frontend.Mobile.Views.Cards.EntryCardView">
    <StackLayout 
        Padding="5,3"
        Orientation="Horizontal"
        Spacing="0">
        <Image 
            Margin="5"
            Aspect="AspectFit"
            Source="{Binding LeftIcon}"
            IsVisible="{Binding LeftIcon, Converter={StaticResource NotNullOrEmptyConverter}}" />
        <Entry
            Text="{Binding Text}"
            HorizontalOptions="FillAndExpand"
            IsPassword="{Binding IsPassword}"
            Placeholder="{Binding Placeholder}"
            TextColor="{StaticResource Gray900}"
            PlaceholderColor="{StaticResource Gray300}" />
        <Image 
            Margin="5"
            Aspect="AspectFit"
            Source="{Binding RightIcon}"
            IsVisible="{Binding RightIcon, Converter={StaticResource NotNullOrEmptyConverter}}" />
    </StackLayout>
</frames:PrimaryCardFrame>
